extends layout

block content
  h1= title
  p Welcome to #{title}
  form
    input(type='button', value='Books', onclick="window.location.href='/books'")

  form
    input(type='button', value='Authors', onclick="window.location.href='/authors'")
